function add_vehicle_button(env) {
	env.add_vehicle();
}

function add_source_button(env) {
	env.add_source();
}

function remove_vehicle_button(env) {
	env.remove_vehicle();
}

function remove_source_button(env) {
	env.remove_source();
}

function refresh_button() {
	location.reload();
}